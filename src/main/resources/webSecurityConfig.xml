<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:context="http://www.springframework.org/schema/context"
             xmlns="http://www.springframework.org/schema/security"
             xsi:schemaLocation="http://www.springframework.org/schema/security
      http://www.springframework.org/schema/security/spring-security-4.0.xsd
      http://www.springframework.org/schema/beans
      http://www.springframework.org/schema/beans/spring-beans-4.0.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">


    <http use-expressions="true" auto-config="false"
          create-session="stateless">
        <intercept-url pattern="/**" access="permitAll" method="OPTIONS"/>
        <intercept-url pattern="/health" access="hasRole('ROLE_ADMIN')" method="GET"/>
        <csrf disabled="true"/>
        <http-basic/>
    </http>

    <context:annotation-config/>

    <beans:bean id="passwordEncoder" class="org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder"/>

    <authentication-manager alias="authenticationManager">
        <authentication-provider ref="LocalAuthenticationProvider"/>
    </authentication-manager>


    <beans:bean id="LocalAuthenticationProvider" class="tk.hishopapp.auth.LocalAuthenticationProvider"/>

    <!-- Enable the annotations for defining the secure role -->
    <global-method-security secured-annotations="enabled"/>

</beans:beans>